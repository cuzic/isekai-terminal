[package]
name = "osc-color"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Pure, dependency-free OSC color escape sequence synthesis (tab color, tmux passthrough wrapping, hex color parsing) shared by isekai-ssh and claude-hookd."

[dependencies]
