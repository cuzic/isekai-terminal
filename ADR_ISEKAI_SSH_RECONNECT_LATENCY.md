# ADR: isekai-ssh(Windows)の再接続レイテンシと進捗不可視性の改善

- **Status**: Draft(2026-09-15起草。長い経緯を圧縮して記す——詳細は
  git履歴の旧版を参照:
  (1) keepaliveチューニング案を破棄(§1.2)。
  (2) オーナー側`channel_open_session`等への会計機構
  (`established_clients`/`HANG_PRESUMED_AFTER`/RAIIガード)を
  Round 1〜7かけて磨いたが、直しても新しい穴が見つかり続け、
  「輸送層の生死をSSHレベルの副作用から間接的に推測する」という
  間違った層で問題を解いていたと判断して破棄した(§1.3)。
  (3) ゼロベースで考え直し、`Mutex`→`RwLock`化(施策A)+輸送層状態を
  子プロセスからファイルで伝播(施策B)+入場ゲート(施策C)という設計を
  組んだが、4ラウンドのレビューで施策Aには観測可能な効果がほぼ無く、
  施策Cも無条件`Rejected`だと短い断(より一般的なケース)を悪化させると
  判明し、ゲート条件を`HELLO_ACK_TIMEOUT`の再利用へ修正した(§1.4)。
  (4) ユーザーの指示で、この施策A/B/C自体にも縛られず再度ゼロベースで
  設計をやり直した結果、**(i) オーナーは共有`Handle`の`Mutex`を実際に
  排他保持したままリモート応答を待っているため、輸送層状態ファイルを
  経由せずとも「自分が今クライアントを捌けるか」をローカルで確実に
  知ることができる**(輸送層状態ファイルは丸ごと不要と判明)、
  **(ii) 元の苦情(「動いているか分からない」)の真因は`--isekai-log-file`
  ではなく、mux導入に伴うプロセストポロジ(holderがdetachedかつ
  stdio null)であり、しかもこのリポジトリ自身が`child_stdio.rs`のdocで
  同じ配線切れを既に記録していた**、という2つの発見に基づき、
  現在の設計(§3)へ全面差し替えた。もう1ラウンドのレビューで
  「子プロセスの生バイトを中継する」当初案(P1-b)にデッドロック級の
  実装リスク(パイプの読み手が長時間不在・raw mode下での改行崩れ等)が
  見つかったため、**その部分は撤回し、クライアント自身の経過時間表示
  という単一の低リスクな仕組みへ統合**した(§1.5)。
  次ラウンド確認待ち)
- **対象**(見込み、要精査):
  `rust-core/isekai-ssh/src/native/mux/client.rs`(ハンドシェイク待ち中の
  経過時間ティッカー)、`rust-core/isekai-ssh/src/native/mux/owner.rs`
  (共有`Handle`のロック保持時間を追跡する`busy_since`+入場ゲート、
  **`Mutex`自体は変更しない**)、`rust-core/isekai-ssh/src/log_file.rs`
  (シェル確立前フェーズ限定で端末にも出す`log_line_progress!`)、
  `rust-core/isekai-ssh/src/native/connect.rs`(認証/再デプロイ/
  ダイヤルの各長時間フェーズへの経過時間ティッカーとフェーズ境界ログ、
  `connect_and_authenticate`にタイムアウトは追加**しない**)。
  **輸送層状態ファイル(新規crate module・JSONL・`intent_id`引き回し・
  GC)、`SharedHandle`の`RwLock`化、子プロセスのstderrパイプ中継は
  いずれも本ADRの実装対象から外れている**(§8参照)。
- **入力**: ユーザーからの実機報告(2026-09-15、Windows機`dragonflyg4`で
  「isekai-sshでログインできない」→実際は「すごく遅かったけどつながった」)
  → clipwire実機調査 → コード調査 → ADR起草 → `opus-adversarial-consult`
  Round 1〜7(旧会計機構案の反復修正とno-op化の発見)→ ユーザーから
  「根本原因を考えてゼロベースでもっと良い案を」の指示 → ゼロベース
  設計1回目(施策A/B/C)→ 4ラウンドのレビューで両施策とも見かけ倒しと
  判明 → ユーザーから再度「ゼロベースでもっと良い案を」の指示 →
  ゼロベース設計2回目(本ADRの核)→ レビューで核心の事実認定は正確と
  確認、実装詳細(子プロセスのstderr中継)にリスクが見つかり撤回・
  統合 → 本改訂
- **拘束される既存ルール**: `.claude/rules/always-connects.md`(本ADR
  全体を通じて最も注意すべきルール)、`ADR_MIDSESSION_DISCONNECT_
  RECOVERY.md`(Accepted・実装済み。resume windowの実体)、
  `ADR_ISEKAI_SSH_OBSERVABILITY.md`(Approved・実装済み。
  「`--isekai-log-file`が有効な間は`log_line!`の端末出力を絶対的に
  抑止する」という契約を持つ——本ADRはこの契約を**シェル確立前の
  フェーズに限って**緩和する、明示的な契約変更を行う)、
  `.claude/rules/rust-ssot.md`(「オーナーがクライアントを捌けるか」の
  SSOTはオーナー自身のロック保持状態であり、輸送層の生死を子プロセスに
  問い合わせる必要は無い、という本ADRの結論そのもの)

---

## 1. 背景

### 1.1 経緯

2026-09-15、ユーザーから「Windows機`dragonflyg4`でisekai-sshでログイン
できない」と報告。ヒアリングの結果、実際には「タイムアウトして失敗した」
のではなく「すごく遅かったけどつながった」だった。`dragonflyg4`の
`isekai-ssh.log`(wrapperスクリプトが`--isekai-log-file`で指定している
実ログ)をclipwire経由で確認し、2026-09-15T08:45:07Z〜08:48:18Z(UTC)の
約3分間、以下の実測タイムラインを確認した(**注: 以下は抜粋・要約で
あり生ログそのものではない**——§1.6末尾参照):

```
08:45:07-13  mux owner: 複数クライアントの接続が
             「client connection ended before the remote channel did」で
             次々teardownされる
08:45:16     isekai-ssh起動(1回目の試行)
08:45:46     「the mux holder rejected this connection
             (no response from the owner within 30s)」← 1回目の30秒待ち
08:46:01     isekai-ssh起動(2回目の試行)
08:46:02     WARN isekai_transport::telemetry: candidate attempt
             failure_stage=quic-connect outcome=cancelled
08:46:31     「the mux holder rejected this connection
             (no response from the owner within 30s)」← 2回目の30秒待ち
08:46:46     WARN isekai_transport::telemetry: candidate attempt
             failure_stage=quic-connect outcome=cancelled(同上)
08:46:47     「the cached deployment could not be reached ...
             relay transport failed: transport lost: idle timeout」→
             常時接続原則によるサイレント再デプロイが発火
08:46:47-51  再デプロイ完了
08:46:52     「trying key ...」
             ここから08:48:04まで**72秒間ログ出力が一切無い**
08:48:04     isekai-ssh起動(別/後続の試行)
08:48:17     mux owner: 「the shared SSH connection is gone;
             exiting immediately」
08:48:18     直接接続にフォールバックし「trying key ...」
```

区間の内訳(起点08:45:07から): 1回目の待ち30秒(15%)、2回目の待ち
30秒(15%)、直接接続からダイヤル失敗・再デプロイ判定16秒(9%)、
再デプロイ5秒(3%)、**認証の無音72秒(40%)**、最終確立14秒(8%)、
合計約182秒(残差はteardown〜1回目起動の9秒)。**mux待ちは合計60秒、
全体の約33%**。最大の単一区間は72秒(40%)である。

### 1.2 keepaliveチューニング案の破棄

**当初、以下のように主張していたが誤りだった**:「`keepalive_interval`
=60秒/`keepalive_max`=3(`native/connect.rs:1077-1079`)の組み合わせが
理論上最大180秒の死活検知遅延を生み、これが実測の2回の30秒待ちの真因
である」。russh 0.48.2のソースとの突き合わせで2重に誤りと判明した:
(1)実効値の計算式`(keepalive_max+2)×keepalive_interval`により正しくは
300秒、(2)180秒でも300秒でも「30秒待ちが2回」という観測は等しく
説明できてしまい、keepalive値をチューニングする根拠にならない。

正しい実効値(300秒→20〜25秒)で再計算すると効果は薄く
(`HELLO_ACK_TIMEOUT`=30秒は変わらないため体感は変わらない)、
resume window(既定10日)・スリープ復帰・Unix版との非対称性のいずれの
観点でも実害の方が大きいと判明し、破棄した。「沈黙の長さだけから
輸送層が本当に死んでいるのか、正当にresume中なのかを区別することは
原理的に不可能」という結論は、本ADRの全体を通じて一貫している。

### 1.3 オーナー側会計機構案(旧設計)の破棄

keepalive案を破棄した後、オーナー側の`ctl_forward::request`/
`channel_open_session`に個別のタイムアウトと後始末を付ける設計を
7ラウンドかけて磨き込んだ。キャンセル安全性の欠如、russh 0.48.2の
構造的制約(`channel_open_session`をタイムアウトで打ち切ると回収不能な
リモートチャネルを残す)、それを回避するための`established_clients`
会計の設計自体がno-opになるバグ、と次々に穴が見つかった。この状態は
「輸送層の生死をSSHレベルの副作用から間接的に推測する」という間違った
層で問題を解いていたことの症状だと判断し、ゼロベースで設計をやり直した。

この過程で発見された以下の事実は現在の設計でも引き継がれる制約として
有効:
- `request_pty`/`exec`/`request_shell`は`want_reply=false`で呼ばれて
  おり遠隔応答を待たないため実質ハングしない。実際にハングしうるのは
  `channel_open_session`と`ctl_forward::request`内の
  `streamlocal_forward`だけ。
- `channel_open_session`をタイムアウトで打ち切ってはならない
  (回収不能なリークを生む、russh 0.48.2の構造的制約)。

### 1.4 ゼロベース設計1回目(施策A/B/C)とその破棄

`handle_died`(死活監視)が詰まった操作と同じMutexで飢餓すること・
`channel_open_session`は`&self`しか要求せず排他ロックが過剰である
ことを発見し、`Mutex`→`RwLock`化+輸送層状態ファイルの伝播+入場ゲート
という設計を組んだ。4ラウンドのレビューで以下が判明し、破棄した:

- **`RwLock`化には観測可能な効果がほぼ無かった**: russhのセッションが
  終了する瞬間、`is_closed()`が`true`になる事象と詰まっていた操作が
  `Err`を返す事象は**同一のdropで同時に起きる**ため、今日のコードでも
  その`Err`経路が死活監視より速くholderを畳んでいた。`NativeHandle:
  Sync`という新しい未検証境界・writer starvation・`MaxSessions`超過
  リスクという新しい失敗モードを買うだけの価値は無いと判断した。
- **入場ゲートは、無条件に`Rejected`を返す設計だと断が短い(より
  一般的な)ケースを悪化させた**: 断が`HELLO_ACK_TIMEOUT`(30秒)未満
  なら今日はオーナーの`channel_open_session`がresume完了とともに
  成功し多重化接続を得られるのに、無条件ゲートはこれを`t=0`で追い出し
  より遅い直接接続へ叩き出す。`resume_loop.rs:81`の
  `RECONNECT_NOTIFY_GRACE`(15秒)のdocが「短い断は自己修復が当たり前」
  と明記する通り、このコードベース自身が「短い断のほうが一般的」という
  前提で設計されている。より本質的には「どれだけ待つか」の方針は
  既にクライアントが`HELLO_ACK_TIMEOUT`として持っており、ownerが
  それを横取りするのは`rust-ssot.md`の精神(判断は正しい所有者に置く)
  に反する。

### 1.5 ゼロベース設計2回目(現在の設計)

上記を踏まえ、ユーザーの指示で施策A/B/C自体にも縛られず再度ゼロベースで
設計をやり直した。2つの構造的事実がこれまでの設計を単純化した:

**発見1: オーナーは輸送層状態ファイルを経由せずとも、自分が今クライア
ントを捌けるかをローカルで確実に知っている。**
`ctl_forward.rs:72-73`の`streamlocal_forward`、`owner.rs:366`付近の
`channel_open_session`は、いずれも共有`Handle`の`Mutex`ガードを
**保持したまま**リモート応答を待つ。したがって先行クライアントが
死んだリンク越しの応答待ちで詰まっている間、後続クライアントは
`handle.lock()`で**確定的に**ブロックされる——これは推測ではなく
待ち行列そのものであり、輸送層状態ファイルより厳密で、伝播の遅延や
`intent_id`取り違えといった失敗モードも持たない。**実インシデントの
「2回目の30秒待ち」は、確率的な現象ではなくMutexの待ち行列という
決定論的な機構である**ことが確認できた(`ctl-socket`の有無に関わらず、
`open_result`側のロックで同じ直列化が起きる)。

**発見2: 「動いているか分からない」の真因は`--isekai-log-file`では
なく、mux導入によるプロセストポロジである。**
tsshスタイルのライブ再接続表示(`isekai-pipe/src/resume_loop.rs:
730-745`の`print_reconnect_status`)は既に実装・テスト済みだが、mux
経路ではそれを出力する`isekai-pipe connect`子プロセスがholder
(`DETACHED_PROCESS`、stdout/stderrが`Stdio::null()`)の子であるため
構造的に人間に届かない。しかも**このリポジトリ自身が既に同じ配線
切れを記録している**——`child_stdio.rs`の`resolve_pipe_log_file`の
docは「the native path never pipes the child's stderr … which was the
actual bug this ADR investigates」と明記しており、`ADR_ISEKAI_SSH_
OBSERVABILITY.md`はこの配線切れのうち`log::`経由の出力(`env_logger`)
は`ISEKAI_PIPE_LOG_FILE`で解決済みだが、**`print_reconnect_status`は
`log::`を通らない生の`eprint!`であり、その解決に乗っていない**。

**この2つの発見により、現行案の輸送層状態ファイル(新crate module・
JSONL・`intent_id`引き回し・GC・torn read対策)は丸ごと不要になり、
Win32コンソールタイトルという新しい表示機構を発明する前に、既存の
表示の配線を直すべきだと判断した。** 現行案でしか救えるケースは
「輸送が長時間落ちているが、まだ誰もハンドシェイクを試していない
状態で最初のクライアントが来る」場合のみで、これは後述のP3
(§4「却下する代案」)の「1番目のタブ」問題と同じであり、退行の無い
happy-eyeballs方式の方が上位互換と判断した(今回は見送り)。

### 1.6 実装詳細のレビューで見つかった問題と、その統合による解消

上記2回目の設計は当初、「子プロセスの`resume_loop`が出す生の
stderr行を、holderがパイプで受け取ってクライアントへ中継する」という
形(以下P1-b)を含んでいた。レビューでこれに実装上の重大なリスクが
複数見つかった:

- パイプにした子のstderrを**読む者が、子のspawnからowner起動までの
  間(実インシデントでは最大72秒)存在しない**——バッファが埋まれば
  子が`write`でブロックし、**SSHのバイトを運ぶポンプごと止まる**
  (`always-connects.md`が守ろうとしている当のものを、可視性の改善が
  壊す最悪の形)。
- 中継される行は非TTY形式の素の`\n`だが、クライアントは
  ハンドシェイク**前**にraw modeへ入っているため、素の`\n`は
  復帰を伴わず**階段状に崩れる**。
- 全画面アプリ表示中の確立済みクライアントにも中継が届き、
  画面を上書きしうる。
- holderに新しいパイプハンドルを追加することになり、このリポジトリが
  過去にハンドル継承で実際にhangを踏んでいる前科がある。

これらはいずれも「子プロセスの生バイトをプロセス境界を越えて中継する」
という設計自体に起因するリスクであり、個別に直すよりも**設計を単純化
して撤回する**方が適切と判断した。代わりに、**クライアント自身が
自分の待ち時間を計測して表示する**という、プロセス境界を一切越えない
単一の仕組み(§3.1)に統合した——これは元の苦情(「動いているか
分からない」)に対しては、子プロセスの詳細な状態(resume window残り等)
を見せずとも「経過時間が動いている」ことを見せるだけで十分だという
判断でもある(詳細な状態は将来の拡張候補として§8に記録する)。

**撤回の代償を裏付ける根拠(レビューで指摘)**: この判断が根拠のある
ものである理由は、**撤回で失われる情報は、直接接続経路(前景プロセスが
接続を確立する経路)では今日既に端末に見えている**ことにある——
`child_stdio.rs:90`の`Stdio::inherit()`により、前景プロセスの子は
実コンソールへ直接書ける。したがって撤回で失われるのは**mux/holder
経路における詳細情報だけ**であり、その経路でも§3.1の汎用ティッカー
(適用箇所1)が「動いていること」は見せる——「十分」という判断は
この非対称性(mux経路だけが情報を失っていた)を踏まえたものである。
副次的な利得として、P1-b(撤回済み)が必要としていた
`MUX_PROTOCOL_VERSION`のbumpも不要になった——新旧holderが混在する
期間に全タブが一時的に多重化を失う、という問題ごと消えている。

なお§1.3の72秒の無音についての診断(認証フェーズ`authenticate_
session`での滞留)も、この診断は「08:46:52から08:48:04まで他のログ
行が無い」という要約されたタイムラインへの推論に基づく。**実装前に、
clipwireで生ログの当該区間を無フィルタで再確認すること**を前提条件
とする。

## 2. 狙い

1. **元の一次的な苦情(「動いているか分からない」)に、mux経由の成功
   経路を一切犠牲にせず直接答える**(§3.1)。プロセス境界を新しく
   越えず、既存の表示機構も新設しない。
2. **オーナーが既にローカルで確実に知っている事実(ロック保持時間)
   だけを使って、無駄な待ちを削る**(§3.2)。クライアント自身の
   既存の待ち方針(`HELLO_ACK_TIMEOUT`)を尊重し、短い断を一切
   悪化させない。
3. **観測性を確立する**(§3.3): 認証フェーズのログを追加し、§1.3の
   72秒の原因を実際に確定させる。
4. **認証フェーズの無期限ハングへの対処方針を決める材料を揃える**
   (§3.4): タイムアウトの実装は見送り観測性のみ追加する。

## 3. 提案する施策

### 3.1 経過時間の自己申告表示(プロセス境界を越えない可視化)

**設計原則**: いずれの表示も、他プロセスから情報を受け取らず、
**自分自身が今どれだけ待っているかを自分で計測して表示する**。
これにより§1.6で撤回したP1-b(子プロセスの生バイト中継)が持っていた
リスク(パイプの読み手不在・raw mode崩れ・全画面アプリの上書き・
ハンドル継承前科)が構造的に発生しない。

**適用箇所1: mux ハンドシェイク待ち(実インシデントの2回の30秒待ち)**

`client.rs:268`の`tokio::time::timeout(HELLO_ACK_TIMEOUT,
frame_rx.recv())`を、1秒ティック付きの`select!`に置き換える。
2秒(表示閾値)を超えたら自分のstderrへ1行更新表示:

```
isekai-ssh: waiting for the shared connection to <host>... (12s)
```

- ハンドシェイクは既にraw modeに入っている(`client.rs:163`の
  `RawModeGuard::enable()`)ため、`\r` + `\x1b[K`で1行を書き換える。
- 決着時(`HelloAck`/`Rejected`/タイムアウト)に`\r\x1b[K`で消す。
- `frame_rx.recv()`はmpscなのでキャンセル安全。`select!`に入れても
  部分読みの問題は起きない。
- **`HELLO_ACK_TIMEOUT`の上限自体は一切変更しない**——表示を足す
  だけなので、接続の成否や待ち時間を変えず、退行のしようがない。

**適用箇所2: シェル確立前の長時間フェーズ(認証・再デプロイ)**

同じティッカーの仕組みを、`native/connect.rs`の
`connect_and_authenticate`(認証)・`wrapper.rs::bootstrap_and_
register`(再デプロイ)の非対話区間に適用する。**候補ダイヤル(QUIC
候補レース)はこの一覧から除く(レビューで訂正)**——ダイヤルは
`isekai-ssh`自身のawaitではなく`isekai-pipe connect`子プロセス内で
起き、親から見ると`establish_over_stream`という1つの呼び出しに
畳み込まれている。その`establish_over_stream`は下記のTOFUプロンプト
(除外対象1番)を内包するため、ダイヤル区間だけを取り出してティッカーで
包むことはできない。**実インシデントのダイヤル16秒はこのティッカーでは
可視化されず、§3.4のフェーズ境界ログでのみカバーされる**——ただし
実インシデントの最大区間である72秒は`authenticate_session`
(下記の通り対話を含まない、ティッカーの対象にできる)なので、
「経過時間が動いている」という一次目的はそちらで達成される。

**対象範囲の制限(MUST、レビューで指摘)**: `connect_and_authenticate`
の内部には**人間の入力を待つawaitが4種類**あり、ティッカーの対象から
**明示的に除外する**——これは以前の版で「タイムアウトを付けては
ならないawait」として列挙した集合と完全に同一である:

1. ホスト鍵TOFUの`[y/N]`(`establish_over_stream`内の`check_server_key`
   →`verifier.verify()`、`russh-stream-session/src/lib.rs:313-324`)
2. パスフレーズ入力(`try_encrypted_identity`、最大3回、
   `native/connect.rs:1195-1220`付近)
3. keyboard-interactive(PAM/OTP/2FA、`native/connect.rs:1171-1178`)
4. SSH agent(ハードウェアトークンの物理タッチ/PIN)

1秒ごとに`\r`+`\x1b[K`で行を書き換えるティッカーがこれらと並走すると、
プロンプトの文字列やユーザーが入力中の文字を消してしまう
——`always-connects.md`が唯一の対話例外として守っている経路を、
可視性の改善が壊すことになる。**ティッカーは`authenticate_session`
単体(`russh-stream-session/src/lib.rs:464-494`、内部は
`authenticate_password`/`authenticate_publickey`/
`authenticate_openssh_cert`のいずれかで対話を一切含まない)・
再デプロイのSSH往復にのみ適用する**(候補ダイヤルは上記の通り対象外)。
実装は「プロンプトの前後で停止/再開する」のではなく、**そもそも
対話を含まないawaitだけを包む**形にする(停止漏れが起きないため)。

**mux経路では、このティッカーはholderプロセスで実行されるため人間に
届かない(MUST、レビューで指摘)**: `connect_and_authenticate`は
`owner_hook`発火(`connect.rs:859`)より**前**、すなわちholder自身の
初回SSH認証中に実行される。holderは`Stdio::null()`のdetached
プロセス(`holder.rs:116-117`)なので、適用箇所2のティッカーの出力は
**`NUL`へ行き、誰にも見えない**——これは§1.5でこのADR自身が発見した
配線切れ(F-C)と同じ構造である。**ただし致命的ではない**:
holderが初回認証をしている間、それを起こした前景プロセスは
`mux/mod.rs::dispatch`経由でholderのパイプへクライアントとして接続し、
適用箇所1のティッカー(「waiting for the shared connection…」)の
中にいる。したがって**「どのフェーズか」は分からなくても「動いている
こと」は見える**——本ADRの一次目的(§2-1)はこの場合も満たされる。
holder側のフェーズ名までの可視化は、§3.4のフェーズ境界ログ
(ファイル、`--isekai-log-file`指定時は集約される)に委ねる。
**適用箇所2・3が端末で実際に見えるのは、前景プロセスが接続を
確立している経路(直接接続、およびmuxからのフォールバック後)に
限られる**。

**適用箇所3: `--isekai-log-file`使用時のフェーズ境界メッセージ**

`log_file.rs`に`log_line_progress!`を追加する。既存の`dispatch`と
違い、**ログファイルへ書いたうえで、シェル確立前のフェーズに限り
stderrへも書く**(tee)。適用先は意図的にごく少数に絞る——
「再デプロイを始める/終えた」「鍵で認証を試みている」等、シェルが
立つ前のフェーズ境界だけ。シェルが立った後は一切使わない。
適用箇所2と同じく、**mux経路ではholderが実行するため`NUL`へ行き、
前景プロセスの経路でのみ端末に届く**。

**契約変更の明示(MUST、対象を3箇所すべてに拡大)**:
`ADR_ISEKAI_SSH_OBSERVABILITY.md`は「`--isekai-log-file`が有効な間、
`log_line!`の端末出力を絶対的に抑止する」という契約を持つ。本ADRが
端末へ出すのは**(1)ハンドシェイク待ちのティッカー(適用箇所1)、
(2)長時間フェーズのティッカー(適用箇所2)、(3)フェーズ境界
メッセージのtee(適用箇所3)の3種**であり、いずれも`log_file::
dispatch`を通らない生の`eprint!`(または新設の`log_line_progress!`)
なので、この契約の対象を**シェル確立前のフェーズに限って**緩和する
ものである。シェル確立後は3種とも従来通り一切出さないことを、
共通の不変条件として維持する。

**効果の確認方法**: 実インシデントの2回の30秒待ちは、`client.rs:299`
の`Err(_)`アーム(「no response from the owner within 30s」という
理由文字列を生成する唯一の箇所)でしか発生しないことから、その区間が
必ず適用箇所1のtimeout内であると確定できる——変更後は必ず表示される
(この区間はプロセスによらず必ずクライアント[前景]側で起きる)。
72秒の無音区間については、上記の通り一般にはプロセス依存(mux/
holder経路では前景に届かない)だが、**実インシデントについては
前景プロセスで起きていたと因果の連鎖から確定できる**: 08:46:31に
2回目のクライアントが`HELLO_ACK_TIMEOUT`で諦め`Rejected`を受ける
→ `run_as_client_over`の`Rejected`アームにより**同じ前景プロセスが
直接接続へフォールバック**する → その直接接続が自前の
`isekai-pipe connect`でダイヤルし失敗 → 08:46:47「サイレント再デプロイ」
→ 08:46:47-51再デプロイ → 08:46:52「trying key…」。**08:46:47以降
(再デプロイ5秒+認証72秒)はすべて前景プロセスで起きている**ため、
適用箇所2・3のティッカー/teeは実際にこの区間で見える。

### 3.2 入場ゲート(ロック保持時間がクライアントの待ち方針を超えている場合のみ)

**設計**: `owner.rs`の共有`Handle`(`Mutex`のまま、変更しない)に、
ロック保持の開始時刻を追跡する薄いラッパーを足す:

```rust
struct GatedHandle<H> {
    handle: Mutex<client::Handle<H>>,
    /// ロックを保持し始めた時刻(モノトニック起点からの経過ms)。
    /// `u64::MAX` は「誰も保持していない」——`0`をセンチネルにしない
    /// (レビューで指摘、Low): モノトニック起点(プロセス起動時刻)から
    /// 1ms未満で取得された場合に実際の経過msと衝突しうるため。
    ///
    /// `tokio::sync::Mutex`ではなく`AtomicU64`(または`std::sync::Mutex`)を
    /// 使う(レビューで指摘、MUST)——ゲートの読み取り(`peer_busy_for()`)は
    /// ハンドシェイクのクリティカルパスで呼ばれるため、そこに不要な
    /// `await`を持ち込まない。ロック順序の循環も無い(ゲートは
    /// `busy_since_ms`だけを読み、`handle`のロックを保持したまま
    /// `busy_since_ms`を長く保持する経路も無い)。読み書きとも
    /// `Ordering::Relaxed`で足りる(レビューで指摘、Low)——
    /// 他のメモリ操作との順序関係を要求しない単独のタイムスタンプ
    /// であり、`active_clients`が`SeqCst`を使っているのは別の理由
    /// (複数の原子操作間の順序保証)によるものなので、それに
    /// 引きずられて`SeqCst`にする必要はない。
    busy_since_ms: AtomicU64,
}
```

`lock_tracked()`が返すガードが取得時に`busy_since`をセットし、
`Drop`でクリアする(RAII、panic/cancelでも確実にクリアされる)。
**モノトニッククロックを使う**(壁時計ではない)——このコードベースは
`resume_loop.rs:55-66`でsuspend検知にモノトニックを基準にしており、
一貫性がある。壁時計だとノートPCが10分スリープして復帰した瞬間、
`busy_for`が600秒に跳ねて全新規タブが拒否されてしまう。

`relay_client`の`Hello`検証直後・`ctl_forward::request`より前で:

```rust
if let Some(busy_for) = gate.peer_busy_for() {
    if busy_for >= HELLO_ACK_TIMEOUT {
        write_frame(&mut writer, &Frame::Rejected { reason: format!(
            "the shared connection has had an SSH request outstanding for {}s; \
             continuing on a direct connection. multiplexing resumes automatically \
             on your next connection.", busy_for.as_secs()) }).await;
        return Ok(());   // Err ではない。既知の縮退
    }
}
// busy_for < HELLO_ACK_TIMEOUT、または誰も保持していない: 何もせず素通し
```

- **クライアントへの応答文言**には「直接接続で続行する」ことに加え
  「多重化は次回の接続で自動的に戻る」ことを明記する。
- **`shutdown.notify_waiters()`を呼ばない**。真のチャネルオープン
  失敗専用のままにする。
- **holderを畳まない**。確立済みクライアントのリレーは走り続ける。
- **`ctl_forward::request`/`cancel`への独立予算は導入しない**
  (現行案からの方針転換、明示的に決定): `cancel_streamlocal_forward`
  が詰まってロックを保持し続けるのは、このゲートのセンサーから見れば
  「正しくbusy」である。独立予算で強制的にロックを解放すると、
  実際には詰まっているのに`busy_since`がクリアされ、新規タブが
  死んだリンクへ吸い込まれる——ゲートの正確性を損なう。

**効果の正確な記述**: 発火するのは`busy_for >= HELLO_ACK_TIMEOUT`
(30秒)のときだけであり、「2番目以降のタブ」ではなく**「先行の詰まりが
既に30秒を超えてから来たタブ」**が救われる(先行の詰まり10秒後に
来たタブは`busy_for=10<30`で素通しされ、今日通り自分の30秒を待つ)。
実インシデントでは1回目の試行(08:45:16開始)が2回目(08:46:01開始)の
時点で既に45秒詰まっていたため、2回目は救われる——**実測された
2回目の30秒待ちは消える**。1回目自身の30秒は消えない(先行者がいない
ため`busy_since`が立っていない)。

**残る留保**: 「輸送が長時間落ちているが、まだ誰もハンドシェイクを
試していない状態で、最初のクライアントが来る」場合、`busy_since`は
`None`なのでゲートは素通しする。このケースを埋めるには、待ちを
打ち切るのではなく直接接続を並行して走らせる(happy-eyeballs)方式が
必要だが、対話プロンプトの競合・リモート側の二重ログインという実装
コストがあり、実インシデントでの寄与も限定的(全体の約16%)と見積もら
れるため今回は見送る(§4)。

### 3.3 認証フェーズについて(タイムアウト実装を見送る)

`drive_connect_recovery`(`native/connect.rs:396-440`)は、エラーの
種類ではなく`isekai-pipe connect`子プロセスが書き残す`ConnectOutcome`
という側チャネル信号の有無でリトライ可否を決める。認証フェーズで
詰まっている最中、子プロセスは失敗していないためこの信号は書かれず、
タイムアウトを追加すると`decide_connect_failure_recovery(None, _)`が
`NoRecoverableSignal`となり、リトライも再デプロイもされずプロセスが
終了する。**現状(タイムアウト無し)は72秒待たされたが最終的に接続
できていた**のに対し、この変更を入れると「15秒で接続失敗」に変わる
——`always-connects.md`に真正面から抵触する体感の悪化である。加えて
`establish_over_stream`はホスト鍵TOFU確認(未知ホストへの`[y/N]`
——`always-connects.md`が認める唯一の対話例外)を内部で行い、
パスフレーズ入力・keyboard-interactive(2FA)・SSH agent(物理タッチ/
PIN)も含めタイムアウトの対象に含めてはならない。

**結論**: タイムアウトの実装は見送り、§3.4のフェーズ境界ログと
§3.1の経過時間ティッカーのみを追加する。

### 3.4 フェーズ境界ログの追加(観測性)

`native/connect.rs::connect_and_authenticate`内、
`establish_over_stream`の前後・各`authenticate_session`の開始/終了
(所要ms込み)に`log_line_verbose!`を追加する(§1.3の72秒の原因究明に
必要)。§3.1適用箇所3の`log_line_progress!`と合わせて実装する。

## 4. 却下する代案

- **keepaliveチューニング**: §1.2参照。
- **`SharedHandle`の`Mutex`→`RwLock`化**: §1.4参照。`is_closed()`が
  `true`になる事象と詰まった操作が`Err`を返す事象は同一のdropで
  同時に起きるため、死活検知を速くする効果が無い。`NativeHandle:
  Sync`という未検証境界・writer starvation・`MaxSessions`超過リスク
  という新しい失敗モードを持ち込むだけの価値は無い。§8に将来課題
  として記録する。
- **入場ゲートの無条件`Rejected`化**: §1.4参照。断が短い、より一般的な
  ケースで今日成功するはずの多重化接続をより遅い直接接続へ叩き出す
  純粋な劣化になる。
- **輸送層状態のholderへの伝播(ファイル/JSONL)**: §1.5参照。
  オーナーは自分が捌けるかをローカルで、より確実に、原因を問わず
  (sshd自体が無応答なケースも含めて)知ることができるため丸ごと
  不要。唯一救えないのは「まだ誰もハンドシェイクしていない状態で
  最初のタブが来る」場合で、これはhappy-eyeballs方式(下記)の
  スコープと重なる。
- **子プロセスのstderrをパイプで中継する(P1-b)**: §1.6参照。
  パイプの読み手が長時間不在になりうる(SSHポンプ自体を止めるリスク)・
  raw mode下で改行が崩れる・全画面アプリの表示を壊す・ハンドル継承の
  前科、という複数の実装リスクが見つかり、クライアント自身の経過時間
  自己申告(§3.1)へ統合した。子プロセスの詳細な状態(resume window
  残り等)を見せることに将来価値が出れば、§8の拡張候補として再検討
  する。
- **happy-eyeballs(mux ハンドシェイクと直接接続を並行して走らせる)**:
  「1番目のタブ」問題(輸送が長時間落ちているが誰もハンドシェイクを
  試していない状態)を退行無く解決できる唯一の方式だが、対話プロンプト
  (パスフレーズ/TOFU/2FA)がどちらの経路でも要求されうるため、
  片方が勝った時にもう片方の対話を打ち切ると端末状態が壊れるリスクが
  あり、リモート側の二重ログインも常態化する。実インシデントでの
  寄与も限定的(全体の約16%)と見積もられるため、§3.1/§3.2の実測を
  見てから改めて判断する(§8)。
- **Windowsでもmuxをやめる**: 認証/TOFU/QUICハンドシェイクをタブごとに
  払うことになり、再接続レイテンシはむしろ悪化する。
- **輸送だけを共有しSSHセッションはタブごとに持つ中間形**:
  `quicmux::AnyMuxConnection::open_bi()`で1本のQUIC接続上に複数
  ストリームを開けるため理論上は可能で、本ADRが扱う問題クラス
  (共有Mutexによる直列化)自体が消える魅力的な設計だが、
  `isekai-pipe connect --stdio`/`isekai-pipe serve`をNストリーム対応に
  拡張する規模の変更(Epicクラス)になる。§8に将来課題として記録する。
- **オーナーのリトライ戦略を変える(別ハンドルで再試行)**: holderは
  共有ハンドルを1本しか持たないので成立しない。
- **`HELLO_ACK_TIMEOUT`(30秒)自体の短縮**: オーナー側の正当な仕事が
  数秒かかりうることを踏まえた意図的な選択であり、短縮すると生きて
  いるが遅いオーナーを誤って見捨てるリスクが上がる。§3.2はこの値を
  変更せず再利用するだけであり、この却下と矛盾しない。
- **`RUST_LOG`一発トグルでの統一ロギング**: `ADR_ISEKAI_SSH_EXIT_
  DIAGNOSTICS.md`§3.5で既出の却下理由がそのまま当てはまる。

## 5. 批判的検討

### 5.1 `always-connects.md`との整合

**不変条件**: §3.2のゲートは「まだ`HelloAck`を書いていないクライア
ント」にしか作用せず、確立済みクライアントのリレーループには一切
触れない。さらに(β)条件により、**ゲートは今日のクライアントの待ち
方針より不利な扱いを一度もしない**——断が`HELLO_ACK_TIMEOUT`未満なら
今日通り、以上ならクライアントはどのみち諦めていたタイミングで
一足先にフォールバックできるだけである。§3.1の各表示は接続の成否を
一切変えない。

### 5.2 新しい層に同種の問題を持ち込まないか

1. **`ctl_forward::cancel`(タブを閉じる経路)も同じロックを取るため、
   閉じるタブが死んだリンクで詰まると`busy_since`が伸びる**。結果と
   して新規タブが拒否されるが、これは意味的に正しい(ハンドルは実際に
   詰まっている)。
2. **ゲートによる再デプロイの並行化**: ゲートで弾かれた各タブはそれ
   ぞれ独立したプロセスで直接接続・サイレント再bootstrapを試みる
   (`RedeployGate`はプロセスごとに独立)。断の最中に開いたタブの数
   だけリモートへのSSHログインが増えうるが、壊れはしない。
3. **経過時間ティッカーが端末を汚す可能性**: raw mode下で`\r`+
   `\x1b[K`の1行更新・決着時に消去という、既存の`resume_loop.rs`が
   非mux経路で既に行っている手法をそのまま踏襲する。

### 5.3 本設計が解決しないもの・確実ではないもの(明示)

- **1番目のタブの30秒待ち**: §3.2では消えない。happy-eyeballs
  (§4で却下、§8に将来課題として記録)を採れば消える。
- **§1.3の認証フェーズ72秒の無音の原因**: 特定しない(§3.1/§3.4で
  可視化と記録はする)。§1.6の留保(生ログ再確認)を前提条件とする。
- **リンクは生きているがsshdが応答しないケース**: §3.2のゲートは
  「SSH要求が返っていないこと自体」を見ているため、このケースも
  実は検知できる(輸送層状態ファイル方式では原理的に検知不能だった
  ケース)——ただしそれ以上の対処[何が原因か]は依然行わない。
- **russhの回収不能チャネル問題**: 残る。ただし§3.2のゲートに
  引っかかったクライアントは`channel_open_session`を呼ばないので
  踏まない。
- **Unix版との非対称性**: 変わらない。ただし§3.1の経過時間表示は
  全プラットフォーム共通の仕組みなので、可視性についての非対称性は
  縮まる。

## 6. 検証計画

**P1(挙動不変、必須)**:
- (a) モックownerが無応答のとき、クライアントが2秒後から経過秒数を
  stderrへ書き、決着時に消すこと(`client.rs:593`の既存テストを拡張)。
  1秒ティッカーの実装は`tokio::time::interval`で刻む形にし、
  テストは`tokio::time::advance`を1秒刻みで進めて各tickの出力を
  確認する(一度に`HELLO_ACK_TIMEOUT`分を`advance`すると中間の
  tick出力を観測できないため、実装とテストの両方でこの刻み方を
  明示的に決めておくこと)。
- (b) `--isekai-log-file`有効時に`log_line_progress!`がファイルと
  stderrの**両方**へ書き、`log_line!`は従来通りファイルのみへ書く
  こと(契約変更がシェル確立前のフェーズに限られることの直接検証)。
- (c) シェル確立後は`log_line_progress!`を一切使わないこと。
- (i、MUST、レビューで追加) 正常系(2秒未満で`HelloAck`が返る)では、
  **ティッカーが一度も表示されない**こと(表示閾値の回帰防止——
  「毎回チラつくようになった」という最も報告されやすい退行を防ぐ)。
- (j、MUST、レビューで追加) 適用箇所2のティッカーが、対話プロンプト
  (パスフレーズ/TOFU確認/keyboard-interactive)と並走して表示を
  上書きしないこと(§3.1の対象範囲制限の回帰防止。既存の
  `prompt_passphrase`をモックするテストに「プロンプト中はティッカーが
  書かない」アサーションを足す形で書ける)。
- (k、レビューで追加) holder経路(mux、新規holderを起こす場合)では、
  適用箇所2・3の出力が端末に出ない(前景プロセスには届かず、holder側は
  `NUL`へ行く)ことを事実として確認する——テストとして書くのが難しい
  場合は、§3.1に事実として明記するだけでもよい。

**P2(挙動変更、必須)**:
- (d、MUST) 先行クライアントが`streamlocal_forward`で詰まっている間、
  経過が`HELLO_ACK_TIMEOUT`**未満**なら後続クライアントは素通しされ、
  今日通り`handle.lock()`へ進むこと(短い断を悪化させないことの直接
  検証、§1.4の教訓)。
- (e、MUST) 同じく**以上**なら、後続クライアントが
  `channel_open_session`を**一度も呼ばずに**即座に`Rejected`を受け、
  理由文字列に「多重化は次回の接続で自動的に戻る」旨が含まれ、かつ
  **確立済みクライアントのリレーが継続する**ことを確認する。
- (f) `busy_since`が、ロックを保持していたタスクがpanic/cancelされた
  場合も確実にクリアされること(RAIIガードの回帰防止)。
- (g) 真のチャネルオープン失敗では従来通りholderが即座に終了する
  こと。
- (h) モノトニッククロックを使っていることの確認——`tokio::time`の
  テストクロックでスリープ相当の時間ジャンプをシミュレートしても
  `busy_for`が不当に膨れ上がらないこと(壁時計を使った場合の退行
  防止)。

**反証テスト(既存、継承)**:
- 輸送層を60秒程度切断→復帰させ、既存のシェルセッションが生き残る
  こと(holderが自殺しないこと)を確認する。
- スリープ→復帰させ、タブが生き残ることを確認する。

**単体テストの実現性**: いずれもin-processのモックsshd(`owner.rs:
1012,1024`の既存テストと同じ枠組み)+`tokio::time`のテストクロックで
書け、実際のWindows名前付きパイプも実ネットワークも要らない
——`.claude/rules/prefer-gh-actions-over-local-cargo`の制約下で
Linux CIで全て検証できる。

**実機確認**: clipwire経由でWindows実機に配布し、意図的な切断からの
再接続時間を、本ADR§1.1のタイムラインと比較できる形で記録する。
P2の効果(先行の詰まりが30秒を超えた後に来たタブが即座にフォール
バックする)は、意図的に輸送を落としてタブを2つ開くことで再現できる。

## 7. 実装順序

1. **§3.1適用箇所1(mux ハンドシェイク待ちのティッカー)**: 最小
   (`client.rs`のみ)、プロトコル変更なし、既存テストへの追記で検証
   可能。実インシデントの2回の30秒待ちを即座に可視化する。
2. **§3.1適用箇所3+§3.4(フェーズ境界ログとtee)**: `log_file.rs`+
   適用箇所の選定。ここまでで72秒の原因を実測する材料が揃う。
3. **§3.1適用箇所2(認証/再デプロイ/ダイヤルのティッカー)**: 2の
   知見を踏まえて長時間フェーズを特定してから入れる。
4. **§3.2(入場ゲート)**: `owner.rs`のみ、~30行。1〜3が安定してから
   投入し、実機で「先行の詰まりが30秒を超えてから来たタブ」が実際に
   救われることを確認する。

各段が独立に価値を持ち、1〜3は接続の挙動を一切変えないので、
「効かなかった」というリスクが構造的に存在しない。

## 8. 将来課題として記録(本ADRのスコープ外)

- **`SharedHandle`の`Mutex`→`RwLock`化**: §1.4/§4で却下した通り、
  「バグ修正」としては正当化できないため実装対象から外したが、
  `relay_loop`の生死確認が新規クライアントのハングに巻き込まれること
  や、健全時の複数タブ同時ハンドシェイクの並列化が実測で問題になった
  場合、独立したリファクタとして再評価する。再評価時は
  `NativeHandle: Sync`のCIコンパイル確認、`ctl_forward::request`の
  書き込みロック取得自体の有界化、並行`channel_open_session`による
  `MaxSessions`超過リスクの検討が必要。
- **子プロセスの詳細な輸送層状態(resume window残り等)をクライアントへ
  見せる**: §1.6で撤回したP1-bの発展形。子プロセスのspawn直後から
  drainする読み手を用意し、raw mode向けに再レンダリングし、制御文字を
  サニタイズする実装(§1.6で見つかった問題への対処)が前提になる。
  §3.1の経過時間表示で当面は十分と判断し、追加の価値が実測で確認
  できてから着手する。
- **happy-eyeballs(mux ハンドシェイクと直接接続の並行実行)**:
  §4参照。「1番目のタブ」問題を退行無く解決する唯一の方式。対話
  プロンプトが不要と判定できる場合(既知ホスト+暗号化不要の鍵)に
  スコープを絞れば安全に実装できる可能性がある。§3.1/§3.2の実測後に
  必要性を再評価する。
- **輸送だけを共有しSSHセッションはタブごとに持つ中間形**: §4参照。
  `isekai-pipe`側のNストリーム対応が前提でEpicクラスの規模。
- **russh側の本質的修正**: `channel.send(..)`が失敗した場合に
  `CHANNEL_CLOSE`を送信するようupstream(またはvendor/fork)を直す
  こと。将来誰かが「`channel_open_session`にタイムアウトを付ければ
  もっと速いのでは」と再導入を試みたときのために、この理由をコード上の
  コメントとして`relay_client`に残すこと(MUST)。
- **Unix版との非対称性**: `ssh(1)`の`ServerAliveInterval`は既定0で、
  輸送層resumeに全面的に委ねている。本ADRはkeepalive値自体には触れ
  ないため、この非対称性は現状のまま維持される。
- **`russh::client::Config::inactivity_timeout`が未設定**: keepaliveとは
  別のノブが存在し、本プロジェクトでは使っていない。

## 9. 設計を通じて得られた教訓(この節を消さないこと)

本ADRに至るまでに10ラウンド以上のレビューを経て破棄した代案
(keepaliveチューニング、オーナー側の会計機構、`Mutex`→`RwLock`化、
輸送層状態ファイル、子プロセスのstderr中継)は、いずれも次の3つの
うち少なくとも1つを含んでいた:

1. **他プロセス/他タスクの状態を、直接観測せずに推測する**
   (輸送層が死んでいるかをSSH操作の副作用から推測する、
   `intent_id`ファイル経由で子の状態を推測する等)。
2. **沈黙の長さという閾値で、待ちを一方的に打ち切る**
   (keepalive短縮、無条件`Rejected`ゲート)。
3. **表示・状態共有のためだけに、新しいプロセス境界・IPC・
   プロトコルフレームを追加する**(輸送層状態ファイル、子プロセスの
   stderrパイプ中継)。

最終的に採用した設計(§3.1の自己申告ティッカー、§3.2のローカルな
ロック保持時間ゲート)は、この3つのいずれも含まない: (1)は
「自分自身が今どれだけ待っているか」「自分が今実際に握っている
ロックをどれだけ保持しているか」という、**プロセス/タスク自身が
既に確実に知っている事実**だけを使う。(2)は`HELLO_ACK_TIMEOUT`という
**クライアント自身が既に持っている既存の待ち方針**を再利用するだけで、
新しい閾値を発明していない。(3)はいずれの施策も新しいIPC・ファイル・
プロトコルフレームを追加しない。今後この領域に手を入れる実装者は、
新しい代案がこの3つのいずれかに該当していないかを最初に確認すると、
同じ回り道を避けられる。
